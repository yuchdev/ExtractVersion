{
  "release": {
    "download_link": "https://packages-s3-useast1-any.s3.dualstack.us-east-1.amazonaws.com/{package_name_dash}/{package_name}-{version}-py3-none-any.whl"
  },
  "releases": {
    "1.4.0": {
      "release_notes": [
        "Tooling-only release: no changes to the extract_version package itself (validate_version, extract_version, sort_versions, available_versions, get_last_version, and the extract-version CLI are all identical to 1.3.0)",
        "Replaced the handwritten release_package.py script with the release-saga PyPI package (pip install release-saga) - a generalized, same-author reimplementation with a similar CLI (the wheel build always runs first; --local-install, --local-dev-mode, --upload-s3, --create-release, --publish-pypi) and the same Saga-style rollback pipeline",
        "Added a [tool.release-saga] table to pyproject.toml to preserve this repo's existing release conventions that don't match release-saga's defaults: git_tag_template = \"release.{version}\" and s3_bucket = \"packages-s3-useast1-any\"",
        "release-saga self-installs its own build/twine dependencies at runtime, so setuptools/wheel/build no longer need to be pre-installed separately to cut a release - only release-saga itself does",
        "CI now installs release-saga and runs release-saga --local-install instead of python release_package.py --mode install",
        "Pinned release-saga>=1.2.0,<2 (in pyproject.toml's dev dependency group and in CI): 1.2.0 is a breaking release that removed --mode, so the unpinned install would otherwise have broken the build",
        "Updated README's Dependencies and Releasing sections, CLAUDE.md, and the repo's threat-model doc to reference release-saga instead of the removed script",
        "RELEASE_NOTES.json's format and historical entries are unchanged"
      ]
    },
    "1.3.0": {
      "release_notes": [
        "Version grammar is now strictly X.Y or X.Y.Z (base-10 integer segments); a bare single integer like \"1\" is no longer treated as a version",
        "validate_version() now requires an exact match: strings with trailing or leading garbage (\"1.0.0abc\", \"1.0beta\", \"v1.0\") are correctly rejected instead of matching on a valid-looking prefix",
        "sort_versions() and get_last_version() now compare differing-length versions (\"1.0\" vs \"1.0.0\") as numerically equal and break ties deterministically by the version string itself, instead of depending on input or filesystem order",
        "available_versions() no longer collapses every unparseable name onto a shared blank \"\" key; unparseable entries are filtered out of the result instead",
        "get_last_version() now raises a clear, documented ValueError when no valid version is found or when neither versions_list nor versions_path is given, instead of an incidental IndexError/TypeError",
        "extract_version(pattern=...) now has a fully documented error contract: a pattern with zero or more than one capture group raises PatternArityError, invalid regex syntax raises re.error, and everything else that fails to find a version returns \"\"",
        "The CLI's exit-code contract is now explicit and documented: 0 for full success, 1 for a semantic or partial failure, 2 for missing/invalid input - visible in extract-version --help",
        "extract/validate keep their partial-success behavior for multi-name calls (valid results still print, one stderr line per failure), now formally part of the documented contract",
        "Fixed available's output (both plain-text and --json) to sort by the same numeric version-sort rule as the rest of the library, instead of lexicographically by string, so \"1.9\" now correctly precedes \"1.10\"",
        "A bad --path on available/last-version (a nonexistent path, a file instead of a directory, or a permission error) now fails cleanly with a friendly message and exit code 1, instead of printing a raw Python traceback",
        "README now documents the full grammar contract, a Compatibility Policy section, and the CLI's output-format and pattern-error contracts",
        "Test suite significantly expanded: test/test_extract_version.py grew from 9 to 45 tests, test/test_cli.py from 17 to 60 tests"
      ]
    },
    "1.2.0": {
      "release_notes": [
        "Move S3 uploads to the shared packages-s3-useast1-any bucket, under an extract-version/ prefix",
        "Update the AWS mirror link accordingly",
        "Refactor release_package.py: use sys.executable for python/pip, add check=True to subprocess calls, drop unused platform-detection helpers"
      ]
    },
    "1.1.0": {
      "release_notes": [
        "Rename the module",
        "Add inventory example"
      ]
    },
    "1.0.1": {
      "release_notes": [
        "First release version",
        "Github Action CI set up",
        "Fix license information"
      ]
    },
    "0.9.7": {
      "release_notes": [
        "Possible to report release using command: release_package.py --mode reinstall --s3-upload --create-release"
      ]
    },
    "0.9.4": {
      "release_notes": [
        "First working version",
        "Introducing release notes"
      ]
    }
  }
}
